#include "main.h"
#include <stdio.h>
#include <string.h>

/**
 * _strlen - function return the length of string
 * @s: parameter for pointer
 * Return: 0
 */
int _strlen(char *s)
{
	int i = 0;

	while(*s[i] != '\0')
		c++;
	return c;
}
