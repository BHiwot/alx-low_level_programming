#include "main.h"
/**
 * *_strcat - function concatenates two string
 * @dest: first input
 * @src: second input
 * Return: string
 */
char *_strcat(char *dest, char *src)
{
	int i, j;

	for (i = 0; dest[i] != '\0'; i++)
	{
	}
	for (j = 0; (dest[i + j] = *src++) != '\0'; j++)
	{
	}
	return (dest);
}
