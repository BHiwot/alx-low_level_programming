#include "main.h"
/**
 * *_strcat - function concatenates two string
 * @dest: first input
 * @src: second input
 * Return: string
 */
char *_strcat(char *dest, char *src)
{
	int i;

	int j = strlen(s);

	for (i = 0; s1[i] != '\0'; i++)
	{
		s[i + j] = s1[i];
	}
	s[i + j] = '\0';
	return (0);
}
