#include <stdio.h>

/**
 * main - writes its name
 * @argc: argument count for main
 * @argv: array of pointer
 * Return: 0
 */
int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
