#include "dog.h"
#include <stdio.h>
int _strlen(char *s);
/**
 * *new_dog - ...
 * @name: ..
 * @age: ..
 * @owner: ..
 * Return: ..
 */
dog_t *new_dog(char *name, float age, char *owner)
{
	if (d != NULL)
	{
		d->name = name;
		d->age = age;
		d->owner = owner;
	}
}
