#include "main.h"

/**
 * _isdigit - function that checks for digit
 * @c: single parameter
 * Return: return 1 if c is a digit otherwise 0
 */
int _isdigit(int c)
{
	int c = 0;

	while (c < 10)
	{
		retrun (1);
		c++;
	}
	return (0);
}
