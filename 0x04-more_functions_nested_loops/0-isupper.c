#include "main.h"
/**
 * int _isupper - for checking uppercase letter
 * Return: return 1 if c is uppercase or 0 otherwise
 */
int _isupper(int c)
{
	int c;

	if
	{
		for(c = 'A' ; c <= 'Z' ; c++)
			return (1);
	}
	else
	{
	return (0);
	}
}
